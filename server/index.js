const express = require('express');
const app = express();
const port = 3000;

// look at the FEC repo to see what they are requesting
// use express.js router
// create a local server
  // connect the front end routes to the back end


// *************
// Server
// *************
  app.listen(port, () => {
    console.log(`Listening on http://localhost:${port}`)
  })